kind: ladder
version: "1.0"
rungs:
  # X0(プレス機からの搬送許可)を検知したらコンベア起動(Y0)
  - "[X0 OR Y0] AND NOT M2 --(Y0)"
  # コンベア起動(Y0)から5秒間搬送する
  - "TON T1 Y0 5000 M2"
  # 搬送が終わったら「搬送完了通知(Y1)」をプレス機へ送る
  - "[M2] --(Y1)"
  - "END"