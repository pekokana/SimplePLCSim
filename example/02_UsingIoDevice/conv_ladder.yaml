kind: ladder
version: "1.0"
rungs:
  # 1. プレスからの搬送指令(X10)で搬送中(M15)を保持。完了信号(Y0)が出るまで継続。
  - "[X10 OR M15] AND NOT Y0 --(M15)"
  # 2. 搬送開始から5秒後に搬送完了内部信号(M16)をON
  - "TON T1 M15 5000 M16"
  # 3. 搬送完了(M16)を外部出力(Y0)としてプレス機へ返す
  - "[M16] --(Y0)"
  - "END"