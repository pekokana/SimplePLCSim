kind: ladder
version: "1.0"
rungs:
  # 1. 搬送許可入力を X10 から X1 に変更
  - "[X1 OR Y0] AND NOT M2 --(Y0)"
  # 2. TONの完了リレー
  - "Y0 --(TON T1 5000) --(M2)"
  # 3. 完了通知(Y1)の維持条件も X1 に変更
  - "[M2 OR Y1] AND NOT X1 --(Y1)"
  - "END"