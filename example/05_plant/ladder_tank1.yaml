kind: ladder
version: "1.0"
rungs:
  # 燃料タンク1の液位が低下(X0)したら給液ポンプ(Y0)を起動、満杯(X1)で停止
  - "[ [ X0 OR Y0 ] AND NOT X1 ] --(Y0)"
  # 供給中の積算（D0 = 残量）※DeviceSimから減算される想定
  - "[ Y0 ] --(D0=D0+1)"
  # 燃料不足(D0 < 100)でSCADA用アラームフラグ(M10)を立てる
  - "[ D0 < 100 ] --(M10)"
  - "END"