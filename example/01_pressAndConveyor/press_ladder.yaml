kind: ladder
version: "1.0"
rungs:
  # X0(起動)でプレス開始、M0(プレス中)を維持
  - "[X0 OR M0] AND NOT M1 --(M0)"
  # プレス開始から3秒後にプレス完了(M1)
  - "TON T0 M0 3000 M1"
  # プレス完了(M1)したら「搬送許可信号」を外部(Y0)に出力
  - "[M1 AND NOT X1] --(Y0)"
  # コンベア側から搬送完了(X1)を受け取ったら、プレスサイクルをリセット
  - "[X1] --(M10)" # 内部リセット用
  - "END"