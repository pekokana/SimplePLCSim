kind: ladder
version: "1.0"
rungs:
  # 1. 材料投入(X0)されたら加工開始(M0)
  - "[ X0 OR M0 ] AND NOT T0 --(M0)"
  # 2. 加工中(M0)で温度(D0)が80度未満ならヒーター(Y0)ON
  - "[ M0 AND D0 < 80 ] --(Y0)"
  # 3. 80度に達したら攪拌(Y1)を10秒間(10000ms)実行
  - "[ D0 >= 80 ] --(TON T0 10000)"
  - "[ M0 AND NOT T0 ] AND D0 >= 80 --(Y1)"
  - "END"