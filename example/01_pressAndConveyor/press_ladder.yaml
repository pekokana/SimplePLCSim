kind: ladder
version: "1.0"
rungs:
  # 1. 起動(X0)。リセット信号を X10 から X1 に変更
  - "[X0 OR M0] AND NOT X1 --(M0)"
  # 2. TON完了リレーを M16 から M5 など範囲内に変更
  - "M0 --(TON T0 3000) --(M5)"
  # 3. 搬送許可(Y0)の維持条件も X1 に変更
  - "[M5 OR Y0] AND NOT X1 --(Y0)"
  - "END"